<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>My WebPage</title>
  </head>
  <body>
    <h1>This is the About Page, you can see the list of the people that have been added.</h1>
    <a href="/about/new">Add one here</a>
    <% if(DB.users.length > 0) { %>
      <% for(var i in DB.users) { %>
        <ul><h3>Person <%= DB.users[i].id %> Data</h3>
          <a href="/about/<%= DB.users[i].id %>"><button>Show</button></a></li>
          <li>First Name: <b><%= DB.users[i].firstName %></b>
          <li>Last Name: <b><%= DB.users[i].lastName %></b>
          <li>Email: <b><%= DB.users[i].email %></b>
        </ul>

      <% } %>
    <% } else { %>
        <div>No users found here.Try to add someone.</div>
      <% } %>
  </body>
</html>
