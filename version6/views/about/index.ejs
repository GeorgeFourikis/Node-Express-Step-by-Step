<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>My WebPage</title>
  </head>
  <body>
    <h1>This is the About Page, you can see the list of the people that have been added.</h1>
    <a href="/about/new">Add one here</a>
    <% if(DB.length > 0) { %>
      <% for(var i = 0; i < DB.length; i++) { %>
        <ul><h3>Person <%= i+1 %> Data</h3>
          <a href="/about/<%= i %>"><button>show</button></a></li>
          <li>First Name: <b><%= DB[i].firstName %></b>, ACTIONS: <span>
          <li>Last Name: <b><%= DB[i].lastName %></b>
          <li>Email: <b><%= DB[i].email %></b>
        </ul>

      <% } %>
    <% } else { %>
        <div>No users found here.Try to add someone.</div>
      <% } %>
  </body>
</html>
